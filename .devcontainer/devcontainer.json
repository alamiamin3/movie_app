{
    "name": "Vite Docker Development",
    "dockerComposeFile": "../docker-compose.yaml",  // Path to your docker-compose.yml
    "service": "frontend",  // The name of the service from your docker-compose file
    "workspaceFolder": "/app",  // The folder inside the container where your code resides
    "settings": {
      "terminal.integrated.shell.linux": "/bin/bash"
    }
  }
  